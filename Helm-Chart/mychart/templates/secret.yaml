{{- if and .Values.secretEnv.enabled .Values.secretEnv.data }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mychart.fullname" . }}-env
  labels:
    {{- include "mychart.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- toYaml .Values.secretEnv.data | nindent 2 }}
{{- end }}
